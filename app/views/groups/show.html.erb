<h1> <%= @group.group_name %> </h1>



      <% if current_user.id == @group.user_id %>
      <%= link_to "Delete Group", group_path(@group), method: :delete, data: { confirm: "Are you sure you want to delete this event?"} %>
      <% end %>

      <%= link_to "Back to Group Page", groups_path %>




